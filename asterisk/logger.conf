; Minimal logger.conf — we only need the "full" file so entrypoint.sh can
; tail it into `docker logs`. The console channel is harmless if -c isn't
; used (it's simply never attached).
[general]

[logfiles]
full => notice,warning,error,debug,verbose
console => notice,warning,error
