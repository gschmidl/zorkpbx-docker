; Narrow RTP range so it matches exactly what's published in the Dockerfile
; (EXPOSE 10000-10020/udp) and docker-compose.yml. Without this, Asterisk's
; default range is wider than what's actually reachable through Docker's
; port mapping, and call audio goes one-way/silent.
[general]
rtpstart=10000
rtpend=10020
